This is software to sync from an Outlook calendar with MAPI to calendars with CalDAV API's as targets.

Currently supported CalDAV backends: 
  - Google (OAuth)

Mostly Vibe-Coded using Claude, as MAPI is the most painful thing I've ever used, but I wanted to avoid the Outlook object model API, as it causes a security banner to pop up on the first sync after boot.
